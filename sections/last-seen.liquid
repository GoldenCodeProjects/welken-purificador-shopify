{{ 'component-product-card-style.css' | asset_url | stylesheet_tag }}
{{ 'section-last-seen.css' | asset_url | stylesheet_tag }}
<script src="{{ 'component-last-seen-script.js' | asset_url }}" defer=defer></script>

<div class="o-container -medium -narrow">
  <h2 class="c-heading -h2">{{ section.settings.heading }}</h2>
</div>

<div class="o-container -medium -wide u-pb-medium">
  <div id="containerLastSeen"></div>
</div>


{% schema %}
{
  "name": "Vistos recientemente",
  "tag": "section",
  "class": "last-seen",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Titulo",
      "default": "Vistos recientemente"
    }
  ],
  "presets": [
    {
      "name": "Vistos recientemente"
    }
  ]
}
{% endschema %}
